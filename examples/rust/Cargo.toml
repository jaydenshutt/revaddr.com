[package]
name = "revaddr-reverse-example"
version = "0.1.0"
edition = "2021"
publish = false
description = "Minimal RevAddr reverse-geocode sample (reqwest + serde)"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
