# Dart / Flutter sample

```bash
export REVADDR_API_KEY="sk_live_..."
dart pub get
dart run bin/reverse.dart
dart run bin/reverse.dart 37.7749 -122.4194
```

For Flutter map apps, reverse on a button press after the user places a pin. Keep the API key on a server when possible.
